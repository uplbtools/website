---
import OpensInNewTab from "./OpensInNewTab.astro";
---
<header class="main-header">
  <div class="container header-container">
    <a href="/" class="logo" aria-label="uplb.tools home">
      <img
        src="/icon.png"
        alt=""
        width="24"
        height="24"
        class="logo-icon"
        decoding="async"
        aria-hidden="true"
      />
      uplb<span class="logo-dot">.</span>tools
    </a>

    <a
      href="https://github.com/uplbtools"
      target="_blank"
      rel="noopener noreferrer"
      class="btn btn-secondary btn-sm-nav"
      aria-label="uplbtools organization on GitHub"
    >
      <img
        src="/brand/github.svg"
        alt=""
        width="14"
        height="14"
        class="header-github-icon"
        decoding="async"
        aria-hidden="true"
      />
      <span>GitHub</span>
      <OpensInNewTab />
    </a>
  </div>
</header>

<style>
  .main-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 200;
    height: var(--header-height);
    background-color: var(--bg-page);
    border-bottom: 1px solid var(--border-subtle);
  }

  .header-container {
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .logo {
    display: inline-flex;
    min-height: 2.75rem;
    align-items: center;
    gap: 0.375rem;
    font-family: var(--font-display);
    font-size: 0.9375rem;
    font-weight: 700;
    letter-spacing: -0.03em;
    color: var(--text-ink);
    white-space: nowrap;
    line-height: 1;
  }

  .logo-dot {
    color: var(--brand-maroon);
    margin: 0 -0.05em;
  }

  .logo-icon {
    width: 1.5rem;
    height: 1.5rem;
    object-fit: contain;
    flex-shrink: 0;
    display: block;
  }

  .btn-sm-nav {
    min-height: 2.75rem;
    padding: 0.4rem 0.75rem;
    font-size: 0.8125rem;
  }

  .header-github-icon {
    width: 0.875rem;
    height: 0.875rem;
    display: block;
    flex-shrink: 0;
  }
</style>
