---
import OpensInNewTab from "./OpensInNewTab.astro";
---
<section class="hero-section" aria-labelledby="hero-title">
  <div class="container container--narrow hero-container">
    <h1 id="hero-title" class="hero-title">
      Free, open-source tools for UP Los Baños
    </h1>
    <p class="hero-lede">
      Room TBA, the campus map. Gawa ng isko't iska.
    </p>
    <p class="hero-sub">
      Plus food, housing, grades, and a campus directory, all built and run by
      UPLB students. No accounts, no tracking, code on GitHub.
    </p>
    <div class="hero-actions">
      <a
        href="https://room-tba.uplb.tools"
        target="_blank"
        rel="noopener noreferrer"
        class="btn btn-primary"
      >
        Open Room TBA
        <OpensInNewTab />
      </a>
      <a href="#tools" class="btn btn-secondary">See all tools</a>
    </div>
  </div>
</section>

<style>
  .hero-section {
    padding: 3rem 0 2.5rem;
    border-bottom: 1px solid var(--border-card);
  }

  .hero-container {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 6vw, 3rem);
    line-height: 1.05;
    font-weight: 800;
    color: var(--text-ink);
    text-wrap: balance;
  }

  .hero-lede {
    font-size: clamp(1.125rem, 2.5vw, 1.375rem);
    line-height: 1.4;
    color: var(--brand-maroon);
    font-weight: 600;
    text-wrap: balance;
  }

  .hero-sub {
    font-size: 1rem;
    max-width: 36rem;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    margin-top: 0.75rem;
  }

  @media (min-width: 768px) {
    .hero-section {
      padding: 4.5rem 0 3.5rem;
    }
  }
</style>
